import React from 'react'
import MemberContent from './memberContent';

export const Member = (props) => {
    return (
        <div>
            <h1>
            고객님은 회원이십니다.
            </h1>
            <MemberContent />
        </div>
    )
}